[package]
name = "xtask"
license = "MIT"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
argon2 = "0.5"
password-hash = { version = "0.5", features = ["getrandom"] }
rpassword = "7.5"
toml_edit = "0.22"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
